---
description: Pause the active timer. Paused time is excluded from the session total.
argument-hint: "[--at HH:MM | --ago <duration>]"
---

# Track: Pause

Use the Skill tool to invoke the `track` skill with the subcommand `pause` and arguments `$ARGUMENTS`.
