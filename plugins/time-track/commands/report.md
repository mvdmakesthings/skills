---
description: Print an invoice-ready markdown rollup with hours and dollar totals.
argument-hint: "[--week | --month YYYY-MM] [--client <name>]"
---

# Track: Report

Use the Skill tool to invoke the `track` skill with the subcommand `report` and arguments `$ARGUMENTS`.
