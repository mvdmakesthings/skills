---
description: Resume a paused timer.
argument-hint: "[--at HH:MM | --ago <duration>]"
---

# Track: Resume

Use the Skill tool to invoke the `track` skill with the subcommand `resume` and arguments `$ARGUMENTS`.
