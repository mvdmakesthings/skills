---
description: Start a billable timer for the named client.
argument-hint: "<client> [--at HH:MM | --ago <duration>]"
---

# Track: Start

Use the Skill tool to invoke the `track` skill with the subcommand `start` and arguments `$ARGUMENTS`.
